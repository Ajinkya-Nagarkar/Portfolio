import React from "react";

export default function Footer() {
    return (
        <footer className="text-center py-6 text-(--text-primary) text-sm font-mono hover:text-(--accent) transition-colors pb-10">
        </footer>
    );
}